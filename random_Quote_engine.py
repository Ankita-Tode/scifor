import random
def sentence(fruits,reason):
  print("I like most",random.choice(fruits),random.chioce(reason))
def main():
  fruits=["apple", "banana","orange"]
  reason=["keep doctor away","build imunity","having more antioxident"]
  sentence(fruits,reason)
